# Case 04 — Missing diagnostic metrics

Current aggregate mAP is available, but per-class AP, recall, and confidence distributions are missing.

The skill believes these metrics could identify whether failure is class imbalance or calibration.

## Expected behavior

- State exactly which metrics are proposed and why.
- State metric-budget cost.
- State expected CPU resource use.
- Ask for explicit user approval before running analysis.
